VITE_ALLOWED_HOSTS=localhost
VITE_DEV_HOST=0.0.0.0
VITE_PREVIEW_HOST=0.0.0.0

# 管理后端 REST/WS 基址（S1-D2 仪表盘）
# 默认留空 = 同源模式：/api、/ws 由 vite dev server 代理转发到管理后端（目标可用
# VITE_ADMIN_PROXY_TARGET 覆盖，默认 http://localhost:3100），域名/反代部署天然无跨域。
# 仅当前端与管理后端不同源直连时才需要配置（此时后端 CORS 白名单须包含页面来源）。
# VITE_ADMIN_SERVER_URL=http://localhost:3100
