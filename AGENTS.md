# 项目记忆（供 AI 会话自动读取）

## 服务已部署运行中 — 不要重复启动

- 管理平台已通过**宝塔面板**部署并常驻运行，公网地址：`https://5174.one.ms7d99.com/`
- 启动目录就是本仓库根目录，本仓库的修改构建后即作用到该域名
- AI **不要**主动执行 `pnpm start` / `pnpm dev` 来"启动服务"；服务已在跑
- 改动 web 前端后，只需 `pnpm --filter web run build-only` 重建产物即可生效（preview 直读 dist，无需重启服务）
- 改动 server 后端代码时注意：线上是常驻进程，重启需用户确认，不要擅自 kill/重启
- 本地验证用 `pnpm dev`（:5173/:3100）仅当线上域名不可用或需要隔离调试时才启动，用完即停
