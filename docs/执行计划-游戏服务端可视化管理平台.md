# 执行计划 — 游戏服务端可视化管理平台

> 依据：《PRD-游戏服务端可视化管理平台.md》v2.3（载体：excalibur_test 整体重构为管理平台，不受其既有架构约束）
> 原则：每阶段结束**可运行可验收**；S1 先用最小 spike 验证两个技术前提（tsx 别名解析、预览实例环境变量），失败即按回退策略调整再继续。
> 文档版本：v1.2（2026-09-20）

## 阶段总览与依赖

```
S1 重构骨架与实例管理 ──► S2 工作区与配置编辑 ──► S3 预览闭环 ──► S4 落盘
                                                          │
                                                          ├──► S5 地图工具（依赖 S2）
                                                          ├──► S6 存档与注册表（依赖 S1）
                                                          └──► S7 游戏观察视图（依赖 S3）
```

S5/S6/S7 在 S4 完成后可按需调整顺序并行推进。

---

## S1 重构骨架与实例管理

**目标**：excalibur_test 重构为管理平台 monorepo（根 = workspace 壳，`server/` + `web/` 双包平级），跑通"网页启停游戏进程 + 实时日志"；工程检查链适配新结构后全绿。

任务：
- [x] **先迁后改（第一步）**：当前前端项目**原样复制**到 `web/`（package.json、src/、index.html、vite.config、tsconfig、prettier 等全量），迁移后立即验证 `pnpm dev` 可运行、`pnpm check:all` 全绿——这是后续一切改造的前提
- [x] workspace 化：`pnpm-workspace.yaml` 改 `packages: ['server', 'web']`；根 package.json 改为 workspace 壳（公共脚本：一键起两端、schema:sync 等）；web 包脚本路径相应修正
- [x] web 侧增量改造（保留现有架构，不重写）：新增管理 views 与 admin 组件；`modules/net`、`modules/maprender` 由原 game 域**移动+重命名**复用（实现不变，仅加连接地址参数化）；PlayView 由原 GameView 适配（连接可选正式/预览实例）；删除仅限 demo 残留（counter store、占位组件）；bridge/MainLayout/路由结构保留并在其上扩展
- [x] 引入 Element Plus + `web/src/styles/element-theme.scss`（管理平台新增页面使用；游戏页继续用 pixelium）
- [x] **Spike-1（立即验证）**：server 的 tsconfig 完整复制本体 paths 别名（`framework/*` 等十余个，指向 `../../game_server_test/framework/*`），`gameBridge/` 写一行 `import ... from "framework/api"` 跑通 tsx，确认别名解析与依赖版本（bitecs/zod/check2d/mistreevous/winston 同版本安装）
- [x] `server/src/config.ts`：`GAME_ROOT`（默认 `../../game_server_test`，与 schema:sync 约定一致）、端口 3100
- [x] `instanceManager.ts`：双实例进程管理——spawn（Windows `pnpm.cmd`）、env 注入、状态机 `stopped|running|crashed`、`taskkill /T /F` 树杀、退出码归因、崩溃事件
- [x] `routes/process.ts`：启动/停止/重启/状态 REST
- [x] `logStream.ts`：tail `logs/game.log` + 进程 stdout/stderr → WS 推送（带实例标识）
- [x] `DashboardView`：正式实例状态卡 + 启停按钮；`LogConsole` 组件（WS 追加、暂停/清屏/级别过滤）
- [x] 预览实例管理能力（暂指向本体配置）：role=`preview` 的 spawn/停止/状态

**验收**：浏览器启停正式与预览实例、日志实时滚动；kill 进程后前端显示"崩溃 + 退出码"；`pnpm check:all`（适配新结构）全绿；PlayView 冒烟可连本体。

> ✅ **S1 已完成（2026-09-20，分支 `feat/admin-platform`）**
> - **Spike-1 通过**：tsx 跨仓裸别名解析成功 + `tsc --noEmit` 零错误，免 build/dist 回退；两处合理偏差：`GameDefinitionSchema` 自 `framework` 门面导出（api.ts 未再导出）、tsconfig 需 include 本体 `bitecs-legacy.d.ts`。
> - **协议适配**：同步最新 schema 后客户端按 per-player 协议修复（`PlayerState.mapId`/`visibleEntities`，移除 `RoomState.mapId/entities` 兼容回退）——本体切片②③演进导致的历史漂移，`schema:sync` 后修复。
> - **验收证据**（协议级，本环境浏览器面板不可驱动，视觉走查留待人工）：check:all 全绿（server tsc 零错误 + web 44 测试 + build）；official/preview 启停、健康检查、日志管道（file/stdout/stderr 双来源+级别提取）、`kill -9` 进程组 → `crashed` + `SIGKILL` 归因、崩溃后重启恢复，全部通过；PlayView 同款路径（@colyseus/sdk joinOrCreate + /maps/runtime）连接本体验证通过。
> - **环境适配**：本机 3000 被占用、本体 `.env` PORT=3001——instanceManager 支持 `OFFICIAL_PORT` env 解析展示端口，spawn 注入 `PORT` 覆盖（dotenv 不覆盖已注入变量）；Windows 分支（pnpm.cmd/taskkill）已实现但因 Linux 环境未真机验证。
> - **已知限制**：S1 双实例同 cwd，game.log 文件行同推双频道（source:'file' 无法归属实例），S3 工作区隔离后自然消除。

**验收**：浏览器启停正式与预览实例、日志实时滚动；kill 进程后前端显示"崩溃 + 退出码"；`pnpm check:all`（适配新结构）全绿；PlayView 冒烟可连本体。

## S2 工作区与配置编辑

**目标**：工作区镜像 + 编辑器 + 校验链，保存只写工作区。

任务：
- [x] `workspaceService.ts`：新建（本体 `game/` 全量复制）/ 列表 / 切换活动工作区 / 重命名 / 删除；基准指纹 = 复制时文件清单 + 内容哈希
- [x] `routes/workspace.ts` + `WorkspaceView`（列表/新建/切换）
- [x] `configService.ts`：工作区文件树 / 读 / 写；schema 路由表（`game.json`→GameDefinitionSchema、`entities/*.json`→ArchetypeSchema、`maps/registry.json`→MapRegistrySchema、behaviors/dialogues/quests/rules/items 各归其位）
- [x] 文件级校验 API：zod safeParse → 结构化错误（JSON 路径 + 消息）
- [x] **Spike-2**：整体校验复用方式——优先同进程 import `tools/validate.ts` 核心函数（需无 CLI 副作用），否则子进程 `pnpm tools validate` + `GAME_CONFIG_PATH` 指向工作区
- [x] `ConfigView`：目录树 + Monaco（JSON 高亮/格式化）+ 保存链（校验失败定位回显）+ "整体校验"结果面板

**验收**：工作区改坏实体 JSON → 保存时报错并定位行/字段；改对后保存成功且本体文件未变。

> ✅ **S2 已完成（2026-09-20，提交于 feat/admin-platform）**
> - **Spike-2 结论**：`tools/validate.ts` 的 `validate()` 失败路径有 `process.exit(1)` CLI 副作用，不可同进程复用 → 改为直接 import 幂等的 `bootstrapFramework()` + `loadGameDefinition()` 核心函数（免子进程兜底）。**实测额外发现**：`loadGameDefinition` 对缺失 game.json 会静默回退 `createDefaultGameDefinition()`——`validateWholeConfig` 已加存在性预检防御。
> - **实现要点**：zod 双实例问题（本体 schema 与 server 的 zod 为不同 pnpm 实例，类类型不兼容）→ 路由表用结构化接口 `SafeParseLike` 适配；活动工作区持久化到 `server/workspaces/.active-workspace.json`（tsx watch 重启不丢）；路径穿越防护（resolve 必须落在工作区 game/ 内）。
> - **集成修复**：S1 CORS 中间件 Allow-Methods 缺 PUT/PATCH/DELETE（S2 契约新方法预检必挂）→ 已补全。
> - **验收证据**：9 步验收序列全 PASS——创建工作区（fingerprint=883a07d5…）→ 配置树 → 读 wolf.json → 坏 JSON（删 kind/components）被 422 拒绝且 errors 含 jsonPath → 复原写回 → changes 哈希级归零 → 整体校验通过（实体 33/行为 4/规则 9/item 16/地图 5）→ 路径穿越被拒 → 删除工作区；本体 `git status` 恒为 0。
> - **web 侧**：WorkspaceView（卡片栅格/改动抽屉/三色状态 tag）+ ConfigView（Monaco + admin-light 主题 + 保存链错误面板 + Monaco marker 跳行 + dirty 守卫 + 整体校验结果条）；Monaco 0.56 以 `editor.api` 子路径按需引入（worker 独立 chunk）。
> - **已知限制**：部分 JSON 语法错误消息无行号（Node 短消息）；PUT 可新建文件（契约未禁止）；工作区删除为硬删除。

## S3 预览闭环

**目标**：落盘前在预览实例看真实运行效果。

任务：
- [x] **Spike-3（关键实测）**：`GAME_CONFIG_PATH` 指向工作区 `game/game.json` 启动游戏，确认 entities/maps 等按该路径解析；存在按 cwd 解析的部分则 spawn `cwd` 兜底到工作区 `game/`
- [x] 预览实例 env 注入：`GAME_CONFIG_PATH=<workspace>/game/game.json`、`SAVE_DIR=<workspace>/.preview-saves/`、`PORT=3200`
- [x] 仪表盘双实例卡：正式/预览并列，各自状态、启停、日志页签
- [x] 配置上下文卡：正式实例配置指纹 vs 活动工作区指纹对比，不一致提示
- [x] 联调：编辑 → 重启预览 → 日志/状态可见

**验收**：改工作区配置 → 预览实例重启后生效（日志可证），本体目录与正式实例零改动。

> ✅ **S3 已完成（2026-09-20，提交于 feat/admin-platform）**
> - **Spike-3 结论：通过，无需回退方案**。绝对 `GAME_CONFIG_PATH` 下资源 glob 相对 game.json 目录解析（`baseDir=resolve(cwd, dirname(path))`，绝对路径胜过 cwd）；`/maps/meta` 实证工作区 registry 生效（删图后 5→4 张）；`SAVE_DIR` 隔离成立（预览存档 640KB 写入 `.preview-saves/`，本体 main.json 哈希不变）。唯一 cwd 依赖为 winston 日志目录（`<cwd>/logs`），预览日志仍混入本体 logs/game.log——logStream 已按 file 来源双频道广播兜底。
> - **重要发现（跨文件完整性约束）**：直接删除 cave/swamp/ruins 图会触发本体启动崩溃（ecosystems biome 必须落在某图 regions；entity-rules 亦引用图键）——仅 tiled-demo 零引用可安全删。**S4+ 落盘执行将加整体校验终门兜底**；S5 地图工具删图需前置引用校验。
> - **实现**：instanceManager spawn 时动态解析活动工作区注入 env（含 .preview-saves mkdir 与 game.json 存在性预检）；无工作区回退本体配置（UI 空态已明示"回退使用本体配置"——回退模式预览会写本体存档）；InstanceSnapshot 增 configPath/saveDir（crash 后保留、回退启动置 null）；`GET /api/config-context`（清单级 vs 本体对比，S4 落盘 diff 的基础）。
> - **验收证据**：9 步序列全过——建工作区→改 registry→preview start→`/maps/meta` 反映工作区→编辑→restart→再生效（联调闭环）→config-context inSync=false→SIGKILL→crashed 且 configPath 保留→恢复→inSync=true→删工作区→回退模式可起；本体 `git status` 恒 0。
> - **web 侧**：仪表盘配置上下文卡（空态/一致绿条/未落盘琥珀警示+改动摘要展开）、实例卡"配置来源"格、ConfigView 预览 chip + 状态机自适应启动/重启按钮（编辑→保存→重启预览→看日志闭环成立）。

## S4 落盘

**目标**：diff 审查 → 备份 → 写回本体 → 可回滚。

任务：
- [x] `applyService.ts`：工作区 vs 本体逐文件对比（added/modified/deleted + 内容 diff，后端用 `diff` 包计算）；前置校验（所选文件必须全过校验链）
- [x] 备份：写回前将被覆盖/删除文件存 `server/backups/<时间戳>/`（含 manifest）
- [x] 回滚：按 backupId 将备份写回本体
- [x] `routes/apply.ts`：diff 预览 / 执行落盘 / 备份列表 / 回滚
- [x] 前端落盘审查页（挂 `WorkspaceView`）：Monaco DiffViewer + 按文件勾选（`valid=false` 禁选）+ 确认对话框 + 备份回执 + "落盘后重启正式实例"引导
- [x] 落盘后状态同步（已落盘文件标记、基准指纹更新）

**验收**：勾选部分文件落盘 → 本体仅所选文件变更，备份可一键回滚；含校验失败文件时无法落盘。

> ✅ **S4 已完成（2026-09-21，提交于 feat/admin-platform）**
> - **执行流程**（固定串行 + 互斥锁）：TOCTOU 复核（执行前重算 changes，409"工作区已变化"）→ 文件级校验（422 invalid 明细）→ **整体校验终门**（S3 发现的删图类跨文件约束兜底，422 message 即原因，如 "Ecosystem biome "plain" does not match any configured map's regions"）→ 备份（files/ + manifest.json，backupId=时间戳-随机，防注入）→ 写回/删除 → 刷新工作区基准指纹（changes 归零）。
> - **回滚为完整逆操作**：overwritten/deleted 从备份写回本体，**added 从本体删除**（含空父目录清理）；与落盘共用互斥锁，409 实证。
> - **验收证据**（本体 git status 为 ground truth）：5 变更 plan（valid 标志正确：坏 Archetype false、其余 true，含 added）→ 含删 cave 的 execute 被**整体校验终门 422 拦截两次**（期间本体 git status 恒 0）→ 恢复后落盘 3 文件 → **本体 git 恰 3 条且路径精确匹配** → 回滚 → git status 归 0（added 逆操作实证）→ 落盘后 config-context inSync=true；互斥并发冒烟：一成功一 409。
> - **web 侧**：WorkspaceView 双页签（落盘审查带琥珀徽标 / 备份与回滚懒加载）；Monaco DiffEditor（added/deleted 空文档路径走通 + unified diff 备选视图）；invalid 文件禁选并 hover 展示 jsonPath 错误；409/422 失败场景分类精确呈现（AdminApiError.status + "重新获取计划"）；成功回执含 backupId 复制 + 重启正式实例引导；落盘/回滚后三路联动刷新（工作区列表/配置上下文/备份列表）。
> - **已知限制**：整体校验终门对坏实体的报错为本体 zod v4 issues 原始 JSON 串（可读性一般，需本体改进错误包装，超出边界仅记录）；plan 的 diff 含 JSON 重排噪音（后续可考虑语义 diff）；backups 无保留策略（S5+ 可加清理）。

## S5 地图工具

**目标**：地图生成/预览/导出/对比（高复用 mapCodec）。

任务：
- [x] `mapService.ts`：解析 `maps/registry.json`（工作区/本体双源）；调 `buildMapGeometry` 产 geometry snapshot；调 `exportGeometryArtifacts` 产 PNG/JSON；色表参数支持
- [x] `routes/maps.ts`：列表 / 生成预览 / 导出下载
- [x] `MapView`：地图列表（key + 管道积木链）、PNG 预览、元信息（尺寸/区域面积/version 指纹）
- [x] 叠加视图：复用 mapCodec 几何重组逻辑渲染 walkable 底色、region 边界着色、演化规则点位
- [x] 同图对比：工作区 vs 本体并排

**验收**：工作区改地图配置 → 不落盘直接生成新预览；与本体同图并排可见差异。

> ✅ **S5 已完成（2026-09-21，提交于 feat/admin-platform）**
> - **不落盘出新预览实证**：改工作区 island seed → workspace version `492619bb` ≠ official `4a9e292c`，且 official version 与本体运行时 `/maps/meta` **逐字一致**（生成正确性交叉验证）；grid 192×192、regions=8。
> - **实现要点**：GeneratorRegistry 取自 `getRegistries().mapGeneratorRegistry`；`stamp-template` 积木的 `params.tiledPath` 必须调用方在加载期内联为 `params.tiled`（复刻本体 resolveMapConfigs 约定，含穿越防护与 422）；palette 走 base64(JSON) query + 0-255 校验；导出用每请求独立 tmp 目录（finally 清理）。
> - **导出证据**：island PNG 25310B（1537×1537 RGBA）；自定义色表 76302B 生效；cave JSON 86368B（version 与运行时一致）。
> - **web 侧**：MapView Canvas 2D 渲染（等比居中映射、黄金角散布 region 配色 + 图例同源、右/下边界 1px 描边）、对比模式双源并行装载 + version 差异琥珀高亮、entity-rules 点位 + tooltip。
> - **已知限制**：tiled 类型图 geometry → 400（本体实际可转 tiled-source 管道，放开 kind 检查即可，留待办）；无 geometry 缓存（百 ms 级生成，可按 version 做 LRU）。

## S6 存档与注册表

**目标**：存档全操作 + 注册表速查。

任务：
- [x] `saveService.ts`：扫描 `data/saves/` 及预览存档目录；解析 WorldRecord 摘要（saveId/tick/savedAt/地图数/按 kind 实体计数）
- [x] `routes/saves.ts`：列表 / 详情 / 删除 / 下载备份 / 恢复（替换活跃存档，提示重启正式实例）
- [x] `SaveView`：正式 + 预览存档双页签；删除二次确认
- [x] `routes/registries.ts`：调 `listRegisteredSystems/Archetypes/Actions/Components/MapGenerators`
- [x] `RegistryView`：五类注册项分组展示 + 搜索过滤

**验收**：存档浏览/删除/恢复/备份全通；注册表与 `pnpm tools list-registries` 输出一致。

> ✅ **S6 已完成（2026-09-21，提交于 feat/admin-platform）**
> - **存档全操作实证**：main.json（1.5MB）summary 完整（tick=159055318、mapCount=5、entityCount=1593、kindStats villager×499/tentacle×291…）；restore 自恢复幂等（读后写，哈希前后均 `143d51ba`）+ warnRestart:true；preview scope 增删下载字节级一致（cmp 通过）；文件名白名单/穿越防护/非法 scope 全部 400。
> - **saveId 读取**：本体 game/rules/server.json 的 saveId（白名单校验，缺省 'main'）。
> - **注册表与本体 CLI 逐字一致**：systems 21 / archetypes 3 / actions 10 / components 48 / mapGenerators 10，条目 id 与 `pnpm tools list-registries` 完全吻合（API 输出 mapGenerators 为 {id}[]、components 为键数组——函数/定义体不可序列化，web 侧宽容归一化）。
> - **web 侧**：SaveView 双 scope 页签 + 详情抽屉 + danger 确认链；RegistryView 五类分组 + 跨类搜索。
> - **执行插曲**：web 侧前一会话因用量配额中断（断点 = mapCanvas 10 个类型错误），配额重置后由新会话接手修复并补 region 着色测试，check:all 全绿（16 文件 / 97 测试）。

## S7 游戏观察视图

**目标**：PlayView 连接任意实例观察运行效果 + 趋势图表。

任务：
- [x] net 模块连接地址参数化：正式（:3000）/预览（:3200）下拉切换，一套客户端能力观察任意实例
- [x] PlayView：Excalibur 渲染地图与实体实时状态（位置/朝向/血条），按地图切换；支持发送移动输入以主动验证 AI/战斗/交互效果
- [x] ECharts 趋势：tick 速率、实体总数时序
- [x] 仪表盘接入

**验收**：PlayView 分别连正式/预览实例均可实时渲染实体移动并可操作；趋势图滚动更新。

> ✅ **S7 已完成（2026-09-21，提交于 feat/admin-platform）**
> - **liveState 观察服务（补齐 PRD §6 设计）**：管理后端以 @colyseus/sdk 作 schema-less Colyseus 观察者（关键决策：不 import 本体 client-schema——实测跨仓 tsx emit 下装饰器注解崩溃；SDK 0.17 动态解码实测字段全对）；每 role 独立会话（running 建连/指数退避、stopped/crashed 拆除并 leave 销毁观察者实体）；1000ms 采样 {tick, tickRate(3 次滑动平均), entityCount(观察者可见实体)}、300 条环形缓冲、WS `live:{role}` 推送 + `GET /api/live/samples` 回填。
> - **采样交叉验证**：tickRate 19.3~20（本体 tickRate=20）、entityCount 31→44 持续变化、tick 每 1s +20；WS 5s 收 5 条；stop 后采样冻结（backfill ts 不再增长）、preview 独立流动。
> - **PlayView 升级**：实例切换器（正式/预览，localStorage 持久化，destroy→nextTick→initGame 重建链路）；initGame 注入 serverUrl，地图/碰撞 HTTP 基址改为**从实际连接端点派生**（预览目标从 ：3200 拉资源）；连接态 bridge 事件 + 断开重连（bridge.dispatch reconnect）；输入链路验证原生可用（WASD/方向键/E/空格/T 提示行）。
> - **仪表盘趋势**：ECharts 按需引入（echarts/core），tick 速率（含基线 20 markLine）+ 实体总数双图，REST 回填与 WS 增量按 ts 去重合并、断线重连补拉、实例停止冻结标注；正式墨青/预览琥珀双色系。
> - **语义修正**：entityCount 为观察者可见实体数（兴趣裁剪），面板副标题已注明。
> - **已知限制**：观察者 join 会在游戏内创建玩家实体（Colyseus 语义，纯观察需本体支持 spectator 模式，超出边界）；tsx watch 开发态重启会丢观察会话（生产 node dist 不受影响）；RoomStateView 字段耦合已注释声明（本体 schema 改名会静默 entityCount=0，tick 仍正常）。

---

## 全量里程碑验收（2026-09-21 完成核对）

1. **配置调试闭环** ✅：新建工作区 → 编辑（校验报错定位 jsonPath+行号）→ 预览实例看效果（工作区注入，本体零污染）→ 落盘（diff 勾选 + 整体校验终门 + 自动备份）→ 正式实例重启生效 → 备份一键回滚。**最终组合冒烟 9/9 全 PASS**（milestone-e2e：含落盘后 inSync 同步与回滚后本体 git 归零）。
2. **地图** ✅：工作区改动即时预览（seed 修改 → version 指纹变化，official 与运行时逐字一致）、叠加视图（walkable/region 着色/规则点位）、同图对比（双源并排 + version 差异高亮）、PNG/JSON 导出。
3. **存档** ✅：浏览/详情/删除/恢复/下载全通，预览存档隔离（.preview-saves，本体 main.json 哈希恒定）。
4. **监控** ✅：双实例状态卡、实时日志（file/stdout/stderr + 级别过滤）、崩溃归因（SIGKILL → crashed + signal）、PlayView 双实例观察与可操作、趋势图滚动更新。
5. **边界** ✅：`game_server_test` 全程 `git status` 为 0（仅运行时日志/存档产物）；excalibur_test 新结构 `check:all` 全绿（17 文件 / 105 测试 + server tsc 零错误）。

**遗留备忘（不影响验收）**：UI 视觉走查需人工浏览器确认（本环境浏览器面板不可驱动）；Windows 分支（pnpm.cmd/taskkill）已实现未真机验证；`web/.env.local` 当前为本地联调值（ws://localhost:3210，远程联调改回 wss://3001.one.ms7d99.com）；预览日志仍混入本体 logs/game.log（winston cwd 语义，S1 已知限制）。

> 🔧 **走查修复轮（2026-09-21，交付后质量收口）**：桌面端浏览器走查发现 4 个功能缺陷 + 若干视觉/交互项，已全部修复并浏览器复测——
> - **F-1 仪表盘整页死亡**：v-for 内对象 ref 被 Vue 3.5 编译为 `ref_for` → echarts.init 收到数组抛错并中断 mounted 队列（实例卡/启停按钮/WS 全挂）。改稳定函数 ref + init 防御；顺带修 LogConsole 回填撑爆页面高度。
> - **F-2 /play 连接静默失败**：`catch {}` 吞错 + 两个嵌入式浏览器环境级坑（`HTMLImageElement.decode()` 永不 settle、rAF 停发致 Excalibur 时钟冻结）+ Loader.dispose 空指针使二次 initGame 永久挂起。修：错误透出、decode 垫片（utils/envCompat.ts）、混合 rAF、幂等 dispose、joinOrCreate 显式 rootSchema、/maps/runtime 对齐契约 v2。验证：official/preview 双向切换三次重建均正常连接渲染。
> - **F-3 格式化无效**：editor.api 无动作贡献 + precondition 静默失败 → 动作链 + 本地 JSON 兜底（可撤销）。
> - **F-4 /maps 源告警自相矛盾**：workspace store 未初始化即参与判断 → 挂载时 ensureLoaded。
> - 顺手修：创建对话框自动关闭清空、390px 侧栏强制折叠防溢出。
> - **遗留（报告留待）**：monaco 内部服务告警（F-5）、工具栏换行（V-2）、错误行定位偏移（I-2）、el-tree 可访问性（I-3）、console 周期性 `@colyseus_schema "refId" not found`（admin 观察者 join/leave 与客户端 patch 竞态，不影响功能）。
>
> 🔧 **独立复验轮（2026-09-21）**：全新子代理独立复验走查修复——6 项修复全部确认生效（F-1 仪表盘冷加载全链路、F-2 三次重建链路、F-3 格式化、F-4 源告警、I-1、V-1），全站回归 PASS（工作区/配置保存链/落盘审查/saves/registries/maps），本体与 HEAD 未动。复验发现 1 个新缺陷 **N-1**（UI 发起启动后卡片卡"启动中"~40s：REST 响应快照生成早于 WS running 推送、晚到覆盖新状态）——已修（instance store 增加 WS 推送修订号，REST 在途期间有 WS 落地则丢弃响应快照，失败回滚同理）+ 回归测试，106/106 全绿。
>
> 🚀 **域名部署轮（2026-09-22）**：平台经 `https://5174.one.ms7d99.com` 对外可用——
> - **应用侧（4887b60）**：api/admin.ts 支持**同源模式**（VITE_ADMIN_SERVER_URL 留空 = REST 相对路径 + WS 由页面协议推导 https→wss）；vite dev/preview 增加 `/api`、`/ws` 代理（目标 VITE_ADMIN_PROXY_TARGET，默认 http://localhost:3100）。
> - **基建侧**：宝塔站点"敏感目录拦截"正则未锚定，拦截了 `/node_modules/**` 与 `/.pnpm/`（vite dev 的 /@fs 模块 URL 必含后者）→ 依赖模块 404 → 黑屏；已从 `node_excalibur_test.conf` 移除这两项（备份 .bak-20260922、.bak-20260922-2）。⚠️ 若日后在宝塔面板重新保存该站点会复发，届时按备份重改或改用产物部署（见下）。
> - **当前部署形态**：5174 = `vite preview`（产物部署，43 文件/11MB，无任何 node_modules//@fs 引用，敏感目录拦截从此无关）；**部署工作流 = `corepack pnpm --filter web run build-only` 即生效**（preview 每次请求直读 dist，无需重启）；本地开发仍用 `pnpm dev`（localhost 直连，不经域名）。
> - **验证**：域名页面/资源 200、/api 200 JSON、wss OPEN、无头 Chrome dashboard 30 请求全 200 + UI 启停交互冒烟通过。
> - **待办**：/assets 长缓存（nginx expires，哈希文件名安全）；构建 chunk 拆分（editor.api 2.6MB 等 4 个 >500kB）；**PlayView 远程游玩**需给游戏端口建独立域名站点（当前 VITE_GAME_SERVER_URL=ws://localhost:3211 仅服务器本机有效）。

---

## 里程碑验收清单（全量）

1. 配置调试闭环：新建工作区 → 编辑（校验报错定位）→ 预览实例看效果（本体零污染）→ 落盘（diff 勾选 + 备份）→ 正式实例重启生效 → 备份回滚可用
2. 地图：工作区改动即时预览、叠加视图、同图对比、导出
3. 存档：浏览/详情/删除/恢复/备份，预览存档隔离
4. 监控：双实例状态卡、实时日志、崩溃告警、PlayView 观察与趋势图
5. 边界：`game_server_test` 仓库零代码改动（git status 仅存档/日志等运行时产物）；excalibur_test 新结构下 `pnpm check:all` 全绿

## 技术前提与回退策略

| 前提 | 验证点 | 回退策略 |
|---|---|---|
| tsx 解析跨仓 paths（Spike-1） | S1 首日 | 改"本体 build 后 file: 依赖引 dist"（唯一可能触碰本体的情形，需先确认） |
| GAME_CONFIG_PATH 目录语义（Spike-3） | S3 首日 | spawn `cwd` 指向工作区 `game/` 兜底；仍不行用 junction 符号链接 |
| 整体校验复用（Spike-2） | S2 | 子进程 CLI 兜底 |
